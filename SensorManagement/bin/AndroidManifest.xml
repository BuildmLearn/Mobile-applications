<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.navneet.sensormanagement"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="15"
        android:targetSdkVersion="19" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SensorActivity"
            android:label="@string/sensor_available_text" >
        </activity>
        <activity
            android:name=".ViewSensorDataActivity"
            android:label="@string/title_activity_view_sensor_data" >
        </activity>
        <activity
            android:name=".SensorDataGraphActivity"
            android:label="@string/title_activity_sensor_data_graph" >
        </activity>
    </application>

</manifest>
